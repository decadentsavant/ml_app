import 'package:flutter/material.dart';

class EditEntryPage extends StatelessWidget {
  const EditEntryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Edit Entry Page'),);
  }
}
